export enum Answer {
    A = "A",
    B = "B",
    C = "C",
    D = "D",
}

export enum Difficulty {
    Easy = "easy",
    Medium = "medium",
    Hard = "hard",
}

export enum GameMode {
    LOBBY = "LOBBY",
    PLAYING = "PLAYING",
    RESULTS = "RESULTS",
}
